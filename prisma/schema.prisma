generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Analysis {
  id        String   @id @default(cuid())
  url       String?
  title     String
  rawText   String
  result    Json
  createdAt DateTime @default(now())
}

